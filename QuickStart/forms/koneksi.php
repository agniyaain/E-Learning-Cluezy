<?php
$host     = "localhost";
$username = "root";
$pass     = "";
$database = "cluezy_ta";

$koneksi = mysqli_connect($host, $username, $pass, $database);

if ($koneksi) {
    //echo "koneksi berhasil";
}
